# config_hotreload

配置与热更新层头文件：配置加载、版本切换、动态参数刷新接口。

## 当前职责

- `config_hotreload_service.hpp`：定义应用配置结构、加载结果、热更新服务和 reload 回调。
- 支持读取 `config/app.yaml`、解析网络/鉴权/存储/AI/TTS 配置、按文件修改时间热更新、环境变量解析。

## 设计边界

配置模块只负责加载和通知，不直接创建数据库连接、AI 客户端或 TTS 客户端。启动层读取配置后，把对应配置注入各业务模块。
