# voice_tts

语音/TTS 层头文件：语音合成请求、音频分发、缓存策略接口。

## 当前职责

- `voice_tts_service.hpp`：定义语音配置、TTS 请求、音频资源、任务状态、TTS 客户端接口和服务入口。
- 支持文本转语音任务提交、C++ TTS 客户端接入、失败重试、超时判断、任务查询和音频缓存。

## 设计边界

本模块不直接依赖 AI 编排模块，也不直接发送网络消息。上层把 AI 文本结果交给 `VoiceTtsService`，再由网络层或资源服务把生成的音频资源返回给客户端。
