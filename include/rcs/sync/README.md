# sync

状态同步层头文件：状态收敛、广播、服务器权威校验接口。

## 当前职责

- `state_sync_service.hpp`：定义玩家位置、动作、服务器状态、状态增量、房间快照和同步服务接口。
- 支持房间注册、玩家加入/离开、状态提交、服务端移动校验、增量提取和快照生成。

## 设计边界

同步模块不直接发送网络消息，也不直接依赖房间模块。上层在房间成员变化时调用同步模块注册玩家，网络层负责把 `StateDelta` 或 `StateSnapshot` 编码并广播给客户端。
