# room

房间与匹配层头文件：匹配队列、房间管理与状态维护接口。

## 当前职责

- `room_match_service.hpp`：定义房间 id、匹配票据、玩家引用、房间成员、房间状态和匹配服务接口。
- 支持主动建房、加入房间、离开房间、关闭房间、准备状态、房间查询、匹配排队和自动成房。

## 设计边界

房间模块不直接处理网络包，也不直接校验 token。上层在完成鉴权后，将 `player_id` 和 `connection_id` 传入房间模块即可。
